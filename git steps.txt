-Create a Account on Git hub at github.com
-Download Git on your desktop
-create new repository,Give it a name.
-open git in your desktop and give a path of your folder which you want to add.
-write following steps on git CMD

 -git config --global user.name "komal"
 -git config --global user.email komalkalokhe@gmail.com

1. git init
2. git clone
3. git status
4. git add .
5. git commit -m "First commit"
6. git remote add origin "copy HTTP code from github account from your remote repository"
7. git push -u origin master
